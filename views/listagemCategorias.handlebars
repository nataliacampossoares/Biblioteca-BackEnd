<h1>Categorias Cadastradas</h1>
{{#if resp.length}}
    <ul>
        {{#each resp}}
        <li>
            <h2>{{nome}}</h2>
        </li>
        {{/each}}
    </ul>
{{else}}
    <p>Não há categorias cadastrados.</p>
{{/if}}
<h1>Cursos Cadastrados</h1>
{{#if resp.length}}
    <ul>
        {{#each resp}}
        <li>
            <h2>{{nome_curso}}</h2>
        </li>
        {{/each}}
    </ul>
{{else}}
    <p>Não há cursos cadastrados.</p>
{{/if}}
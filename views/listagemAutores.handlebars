<h1>Autores Cadastrados</h1>
{{#if resp.length}}
    <ul>
        {{#each resp}}
        <li>
            <h2>{{nome_autor}}</h2>
        </li>
        {{/each}}
    </ul>
{{else}}
    <p>Não há autores cadastrados.</p>
{{/if}}